{
  "$comment": "Defines the three products built from this one source. The build walks the repo root, drops anything matching alwaysExclude (build artifacts + Finder ' 2' duplicates are handled in code), then drops each variant's own exclude globs. 'core' strips the UI layer; 'claude-design' ships everything; 'standalone' ships everything plus the API server in standalone-server/.",
  "source": "..",
  "out": "dist",
  "alwaysExclude": [
    ".git/**",
    ".github/**",
    ".claude/**",
    "variants/**",
    "node_modules/**",
    "**/.DS_Store",
    "**/package-lock.json"
  ],
  "$uiLayerNote": "App/tooling UI + stale build artifacts only. Everything an agent needs to reproduce the system stays in core: the FULL template library (each templates/ folder complete — .dc.html + its support.js/ds-base.js runtime + thumbnail), the component implementations (components/**/*.jsx) and the styles.css token entry point. Stripped: the app shell, Visual Board + editor, ui_kits, the compiled _ds_bundle.js AND _ds_manifest.json (both are this build's compiled output baked to a stale namespace — the host recompiles its own), the .card.html viewer cards (they mount the old namespace), and the icon editor.",
  "uiLayer": [
    "app/**",
    "LinkedIn Visual Designer.html",
    "Visual Board.html",
    "START HERE.html",
    "visual-board.js",
    "board-editor.js",
    "ui_kits/**",
    "_ds_bundle.js",
    "_ds_manifest.json",
    "components/**/*.card.html",
    "components/**/icon-editor.html"
  ],
  "variants": {
    "core": {
      "title": "DS Core (headless)",
      "audience": "The agent inside Claude Design. Load as a design system; no interface.",
      "exclude": ["@uiLayer"],
      "overlay": "overlays/core"
    },
    "claude-design": {
      "title": "Claude Design + UI",
      "audience": "Loads into Claude Design as a full design system, interface included. This is today's shipping product.",
      "exclude": []
    },
    "standalone": {
      "title": "Standalone (Opus 4.8 API)",
      "audience": "Runs outside Claude Design with its own UI, driving claude-opus-4-8 via the Anthropic API.",
      "exclude": [],
      "extraSources": ["standalone-server"]
    }
  }
}
